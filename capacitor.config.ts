import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'nc.digit.jeucartebataille',
  appName: 'Jeu de carte Bataille',
  webDir: 'www'
};

export default config;
