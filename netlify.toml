[build]
  command = "npm run build"
  publish = "www"

[build.environment]
  NODE_VERSION = "22"

# Routage SPA : toutes les URLs renvoient vers index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
